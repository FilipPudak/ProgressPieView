apply plugin: 'android-library'

android {
    compileSdkVersion 19
    buildToolsVersion "19.0.1"

    defaultConfig {
        packageName "com.filippudak.ProgressPieView"
        minSdkVersion 4
        targetSdkVersion 19
        versionCode 1
        versionName "1.0"
    }

}

dependencies {
    compile 'com.android.support:support-v4:19.1.0'
}